import styled from 'styled-components';

export const LivestreamWrapper = styled.div`
  padding: 24px;
  margin-top: 24px;
  margin-bottom: 24px;
  border: 1px solid #fff;
  border-radius: 12px;
`;

export const LivestreamLinkWrapper = styled.div`
  display: flex;
  justify-content: space-between;
  padding: 12px;
  border: 1px solid var(--border-base-color);
  border-radius: 8px;
`;

export const LivestreamLinkIcon = styled.div`
  cursor: pointer;
`;
